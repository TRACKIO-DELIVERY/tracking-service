version: "3.9"

services:
  frontend:
    build:
      context: ./front
    ports:
      - "5173:80"
    restart: always

  backend:
    build:
      context: ./server
    ports:
      - "3000:3000"
    volumes:
      - ./server:/app
    restart: always

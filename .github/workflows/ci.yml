name: CI Tracking Service

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop
  workflow_dispatch:

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
    
      - name: Set up Buildx
        uses: docker/setup-buildx-action@v2
      
      - name: Build and cache tracking service
        uses: docker/bake-action@v6
        with:
          push: false
          load: true
          files: docker-compose.yml
          targets: tracking-server
    
  sync_notify_trackio:
    if : github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    needs: ci
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Sync and notify Track.io
        env:
          PARENT_REPO: TRACKIO-DELIVERY/trackio
        run: |
          curl -L \
          -X POST \
          -H "Accept: application/vnd.github+json" \
          -H "Authorization: Bearer ${{ secrets.GH_TOKEN }}" \
          -H "X-GitHub-Api-Version: 2022-11-28" \
          https://api.github.com/repos/${{ env.PARENT_REPO }}/dispatches \
          -d '{"event_type":"tracking-service-updated","client_payload":\
          {"ref":"'"${{ github.ref }}"'","sha":"'"${{ github.sha }}"'",\
          "repo":"'"${{ github.repository }}"'}'